# Corpus authoring tools

These scripts prepare source data that is committed under `Scripts/verified/`; generated consumers never require network access.

- `parse_expression_docs.mjs` parses a local clone of the docsforadobe expression reference into grouped JSON records with commit and license provenance.

Inspect each tool's header for required local inputs. Review generated corpus changes before committing them.
